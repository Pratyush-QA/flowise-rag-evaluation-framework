[pytest]
asyncio_mode = auto
python_files = *.py
python_classes = Test*
python_functions = test_*
markers =
    flowise: marks tests that require Flowise to be running locally
    ragas: marks RAGAS evaluation tests
    datagen: marks data generation utilities (run separately, not in main suite)
addopts = -m "not datagen"
